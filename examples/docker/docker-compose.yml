services:
  decree:
    image: ghcr.io/jtmckay/decree:latest
    volumes:
      - .:/work
    environment:
      - DECREE_AI=opencode
      - DECREE_DAEMON=true
      - DECREE_INTERVAL=2
      - DECREE_CONTAINER=decree_docker_example
    tty: false
    restart: unless-stopped
